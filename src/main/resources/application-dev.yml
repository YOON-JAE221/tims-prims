# ===========================================
# DEV 환경 (테스트 배포 - DB는 local과 동일)
# ===========================================
server:
  port: 8080
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    min-response-size: 1024

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db.primushome.gabia.io:3306/dbprimushome?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&autoReconnect=true&useSSL=false
    username: primushome
    password: vmflajtm12@@
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000

upload:
  base:
    dir: /web/upload/

site:
  url: http://mwkim-dev.com

logging:
  level:
    com.prims: INFO
    org.mybatis: WARN
    propertyMng: INFO
    propertySearch: INFO
    bbs: INFO
    bbsComMng: INFO
    org.springframework.jdbc.core: WARN
