<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminNewsMng">

	<!-- 뉴스 목록 조회 -->
	<select id="getSelectNewsList" resultType="map">
		SELECT NEWS_CD
		     , NEWS_TITLE
		     , NEWS_URL
		     , RGT_DT
		     , USE_YN
		     , DATE_FORMAT(CRE_DTM, '%Y-%m-%d %H:%i') AS CRE_DTM
		  FROM TB_NEWS
		 WHERE DEL_YN = 'N'
		 ORDER BY RGT_DT DESC
	</select>

	<!-- 뉴스 등록 -->
	<insert id="insertNews" parameterType="map">
		INSERT INTO TB_NEWS (
			NEWS_CD
		  , NEWS_TITLE
		  , NEWS_URL
		  , RGT_DT
		  , USE_YN
		  , CRE_USR_CD
		  , CRE_DTM
		  , DEL_YN
		) VALUES (
			#{newsCd}
		  , #{newsTitle}
		  , #{newsUrl}
		  , #{rgtDt}
		  , #{useYn}
		  , #{sessionId}
		  , NOW()
		  , 'N'
		)
	</insert>

	<!-- 뉴스 수정 -->
	<update id="updateNews" parameterType="map">
		UPDATE TB_NEWS
		   SET NEWS_TITLE = #{newsTitle}
		     , NEWS_URL = #{newsUrl}
		     , RGT_DT = #{rgtDt}
		     , USE_YN = #{useYn}
		     , UPD_USR_CD = #{sessionId}
		     , UPD_DTM = NOW()
		 WHERE NEWS_CD = #{newsCd}
	</update>

	<!-- 뉴스 삭제 (물리삭제) -->
	<delete id="deleteNews" parameterType="map">
		DELETE FROM TB_NEWS
		 WHERE NEWS_CD = #{newsCd}
	</delete>

</mapper>
