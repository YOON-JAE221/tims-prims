<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="comHistory">
	
	<!-- 회사 연혁 쿼리 -->
    <select id="getSelectComHstList" parameterType="map" resultType="map">
        SELECT A.HST_CD
             , A.HST_YR
             , A.HST_CNTS
             , A.SORT
             , A.USE_YN
          FROM TB_COM_HST A
         WHERE A.DEL_YN = 'N'
           AND A.USE_YN = 'Y'
         ORDER BY A.HST_YR DESC, A.SORT DESC
    </select>
	
	</mapper>
	
	