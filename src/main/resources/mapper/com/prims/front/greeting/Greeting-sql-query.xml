<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="greeting">
	
	<select id="selectGreetingPost" parameterType="map" resultType="map">
	  SELECT A.PST_CD
	       , A.PST_CNTS
	    FROM TB_BBS_PST A
	   INNER JOIN TB_BBS_BRD B ON A.BRD_CD = B.BRD_CD AND B.BRD_CD = #{brdCd} AND B.DEL_YN = 'N'
	   WHERE A.DEL_YN = 'N'
	     AND A.USE_YN = 'Y'
	   LIMIT 1
	</select>
	
	</mapper>
	
	