# ===========================================
# PROD 운영 환경 (최적화)
# ===========================================
server:
  port: 8080
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    min-response-size: 512
  tomcat:
    threads:
      max: 30
      min-spare: 3
    connection-timeout: 3000
    accept-count: 50
  servlet:
    session:
      timeout: 20m

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db.primushome.gabia.io:3306/dbprimushome?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&autoReconnect=true&useSSL=false&cachePrepStmts=true&prepStmtCacheSize=250&prepStmtCacheSqlLimit=2048
    username: primushome
    password: vmflajtm12@@
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 10000
      max-lifetime: 1800000
  mvc:
    async:
      request-timeout: 10000

upload:
  base:
    dir: /web/upload/

site:
  url: https://primus-property.co.kr

logging:
  level:
    root: WARN
    com.prims: WARN
    org.mybatis: ERROR
    org.springframework: WARN
    org.apache: ERROR
